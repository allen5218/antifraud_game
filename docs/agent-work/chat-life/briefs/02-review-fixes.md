# Continue same conversation: first implementation NOT accepted

Project C:\Users\kun\.gemini\antigravity\scratch\antifraud_game, same dirty working tree, same C1-C8 spec. Codex independently reviewed completed job implement-mu8cikqp-d2d18e2c. You remain sole application/test writer. Fix findings below as one coherent assignment, preserve user baseline changes. No commit/push/deploy, no Docker, no existing antifraud_dev.db, no real LINE calls. Do not weaken acceptance or call function existence/in-memory mocks full API proof.

## R1 Critical actual reply crashes and invented facts
Codex executed work/chat-review-probe.py (projectless). ALL nine legit/pause stories throw UnboundLocalError for '可以先不要匯嗎？' and '我沒說我要付款，我想先看單據': decision_point unassigned. Seven stories invent $1,000 because amount field aliases miss real facts, including no-payment finale. Eliminate default invented amount/vendor/authorization/urgency. NPC is the friend asking for help, not automatically the scammer/vendor! Reply must use per-story npc_claims and explicit staged allowed claims. Answer multiple intents (negation + request for 單據), ask clarification on uncertain input. Current parse_intent still only keyword substring lists, verify always implies reject and makes verify branch unreachable; implement structured model intent parsing where configured, deterministic negation/question fallback with uncertainty, not merely more keywords. Preserve mixed legitimate/scam reactions; truth must NOT directly choose all 'urgent refusal' vs 'cooperative' templates. No single-tone answer giveaway.

## R2 LLM violates frozen facts
generate_reply passes old persona bundle to old agent WITHOUT constrained story reply plan; validates only full exact forbidden sentences, allowing any paraphrase of truth or changed amount. Build rule-selected plan BEFORE generation. Give model only allowed claims/current disclosure/history, not secret facts. Validate output against bounded allowed content; canonical safe fallback on new numbers/entities/official claims, injection or errors. Return explicit reply_mode rules/model to UI. Tests with malicious/mutating mocked model must run actual generate_reply path (not just pure fallback). No live calls required/claimed; never print keys.

## R3 Real branches, six actual items, episode memory
evaluate_state_branches is ONLY invoked by tests; routes don't read property/vehicle/xp or consume branch outputs. stories.branches/prerequisites/outcomes unused; all required_flags empty; validate_story_catalog claims cycle validation but does none. Implement state-derived reachable actions/dialogue branches in actual API/UI for money/network/handling/home/car/XP/inventory. At least 6 actual item triggers, server inventory required. Nonbuyers retain core verification. Implement prerequisite validation + meaningful cross-contact finale unlock; reject unknown story_id, wrong contact, locked story, direct finale bypass. Prior callback ignores actual last_outcome and falsely thanks player for preventing a loss even when they paid scammer; fix by stored disposition. Use distinct story-specific outcomes in actual results, not generic fraud-type flags. Pause should save/resume SAME episode, not complete it, reveal truth or mint repeated XP/memory. First episode reward + finale reward only once per logical episode/user, not again after cycling/restarting; replay may be practice with no progression farming.

## R4 Tools/evidence/security
verify route never checks tool availability or owned items. Unknown tool succeeds with invented fallback evidence. Reject unknown/unavailable tools, never fall back to unrelated fraud-type facts for story sessions. is_sufficient_evidence ignores story_id/snapshot, only counts hardcoded categories; commemorative plaque + personal records currently returns True! Define per-snapshot claim coverage/source IDs and valid alternative evidence paths. Same record scanned/photo twice is same source; honors/decor never independent verification. Read available tools filtered for THIS story and actual state. Return explicit '遊戲模擬查證' context, not impersonated live official lookup. Legacy session paths safe/readable but don't fabricate unavailable facts.

## R5 Transactions and durable idempotency
request_id fields ignored; expected_revision optional and frontend sends neither. message/verify no row locks/CAS so lost updates. Add persistent action receipt/idempotency (same id/body same stored response, mismatched body conflict), mandatory correct front-end revision/request handling, uniform lock order User -> session -> relations/inventory, refreshing locked objects. No mid-transaction commits in _get_or_create_relation. Judge currently commits wallet before guardian; make full episode/wallet/relation/progress atomic including guardian or safely idempotent. Unknown decisions reject. Concurrent inbox/new must not create duplicate episodes/relations; add DB uniqueness where appropriate. Validate purchases crossuser and concurrency. Retry after lost response must not double charge or silently repeat. Migration must match models and all needed app tables, preserve pipeline tables. Verify fresh PG and upgraded legacy schemas.

## R6 Rewards
Report says blindguess cap100 but multiplies that base above100; enforce final cap, less learning reward for no evidence. Report says fair legit/scam but still uses unequal old reward_legit/reward_win; implement intended equivalent standard for NEW chat only (preserve legacy economy behavior). chapter multiplier currently lost previous cap5 (1.15**completed_chapters raw); preserve existing chapter cap. is_new_referral=any prior completion, effective_tool=any evidence and fullservice=turns<=8 are not actual conditions; derive real referral/action/service objectives. 2.5 finale replaces ordinary chat factor only on first qualified completion, no farming. Item boost only if that ITEM action provides relevant new info. UI MUST show actual reward breakdown and storyline outcome after conclusion and on refresh. Store result receipt.

## R7 No spoilers and grounded content
read_scenario returns learning_objective that literally identifies scam. Remove from active response (only postcase), also remove fraud-type leakage on new chat payloads. Story snapshot must carry version/variant/claims/outcomes/requirements, all runtime grading uses snapshot not mutable catalog. Original stories label original in UI/debrief; do not imply real sourced case. Current content invents regulatory rules/databases, '不可篡改拍立得', scanner detects authenticity, honorary plaque proves trust, spare phone safe malware sandbox; these teach wrong lessons. Replace with modest evidence-preservation / independently found channels / safe nonexecution and uncertainty; generic fictional records with clear simulation, no unsupported legal rulings (e.g. fire-extortion necessarily civil dispute, all transactions definitely legal from registry). If quoting real law verify official source or omit categorical rule. Weird atmosphere OK, evidence/behavior central. Don't frontload full diagnosis/amounts/solution in every opening.

## R8 UI/tests/report
Detail query error currently infinite '載入中'; add proper retry/errors for load/verify/judge/new/purchase preserving input. Mode shown accurately. Wire actual branch actions, paused resume and saved results. Input accessible, mobile no overlay. Three BottomTabs failures caused by THIS rename are not preexisting: update related test expectations and test chat hooks/errors/branch interactions. Compare baseline file snapshot at C:\Users\kun\Documents\Codex\2026-09-18\feature-gameplay-modification-c-users-kun\work\chat-baseline\source (read only) before attributing failures. Unit tests currently test local objects/direct imitation instead of actual routes; add authenticated API integration with server-owned state and concurrent requests. Full SDK regenerate, frontend build, focused lint/types/tests, document genuine existing failures.

## Isolated PostgreSQL available, use it NOW for proof
Native PostgreSQL17.11 at127.0.0.1:55437, user chat_test, trust auth loopback only, database chat_life_acceptance_20260919 created empty. No Docker needed. Binaries C:\Users\kun\Documents\Codex\2026-09-18\feature-gameplay-modification-c-users-kun\work\isolated-postgres\pgsql\bin. Scope all commands explicitly to that host/port/user/test DB. Set POSTGRES_SERVER/PORT/USER/PASSWORD/DB explicitly before importing app; ensure core.db does not silently choose developer SQLite. Use uv for Python. Can create additional chat_life_* ephemeral DBs here for migrations/concurrency. Do not touch other databases. Run actual PostgreSQL concurrent duplicate message/purchase/judge tests, API 403/invalid tool/unowned item, rollback and migration roundtrip. If test infra not usable report concrete error, don't silently use SQLite as proof.

Update implementation-report.md with findings fixed, real exact test results, changed paths, remaining issues; retract inaccurate initial A1-A8 blanket completion. Finish only when runtime behavior tested. Existing user changes preserved. Codex will independently inspect/test your final result.
