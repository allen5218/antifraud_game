# Third assignment: bounded runtime transaction + pause/progression repair

Continue job implement-mu8om50n-182138e6, same repo/worktree and preservation rules. Prior report still inaccurate: NOT all R1-R8 accepted. This assignment is deliberately narrower: finish ACTUAL request concurrency, pause/resume, episode qualification, and their frontend wiring. Do not claim all spec complete. Content/branch engine/LLM hardening will be reviewed separately.

## New independent diagnosis (actual PostgreSQL FastAPI requests)
Codex probe at C:\Users\kun\Documents\Codex\2026-09-18\feature-gameplay-modification-c-users-kun\work\chat-api-review-probe.py; output chat-api-review-result.json.
- Two concurrent messages, different request IDs, BOTH expected_revision=0: BOTH return200 revision1; only ONE message persisted. Your test_pg_cas_revision_concurrency is sequential, not concurrent, and no SQL CAS/lock exists in message/verify.
- POST judge action=pause returns422: schema Literal still excludes pause; purported paused branch is unreachable. Frontend still only safe_exit, completes episode, no resume endpoint/UI.
- New player can POST contact_id=li_li, story_id=cross_brand_charity_finale ->200, skipping all prerequisites and assigning wrong character.
- useScenario.ts STILL sends no request_id or expected_revision anywhere. Receipts cannot protect actual UI. Receipt check before locking can race even when later mutation locks.

## Implement these exact observable behaviors
T1 Transactions: all mutating chat operations serialize in consistent lock order User -> Scenario -> related rows, refreshing identity-map data; alternatively real DB conditional CAS. Receipt lookup MUST be repeated/inside lock after acquiring serialization, then stable saved response. Message/verify/new/inbox/purchase/judge concurrency must not lose updates or duplicate rows. DB unique constraints for contact+user/inventory+user as appropriate. Unknown actions rejected. Failed reply/commit rolls back everything. No post-wallet secondary commits. Same request ID/body returns stored response even after completed; same ID/different body409. Two different concurrent message requests with same expected revision: one200 one409, persisted exactly one. Two same-ID concurrent mutations: both same200 response, one economic/turn effect. No test threading.Barrier inside locked model (would intentionally deadlock); use concurrent requests + delayed mock reply to test serialization.
T2 Real pause/resume: keep safe_exit backward-compatible but for NEW chat map it to nonterminal paused semantics, or add dedicated pause endpoint plus frontend use it. Do not write completed_story_ids, completion outcome, chapter/guardian/relation progression or any cash/XP on pause. No truth, learning objective, debrief, role or answer leaks. Explicit resume returns SAME ID/snapshot/evidence/history/turns; paused episode blocks new replacement for that contact. Repeated pause/resume cannot farm or discard evidence. Old completed sessions remain readable. Add pause/resume controls, errors, accessible labels. Result shown only terminal dispositions.
T3 Qualification/progression: reject unknown story, wrong-contact story and unmet prerequisites on POST/new; auto selection checks full USER-wide completed story IDs for cross-contact requirements. New player cannot finale. Ordinary replay is practice only: no cash, XP, relationship, guardian, chapter or referral farming, not merely wallet0. Persist terminal result and return it on detail/reload, including story-specific outcome/source mark/reward_breakdown. Required conditions from snapshot, not mutable catalog. No claims of a live external lookup.
T4 Frontend: real per-operation stable request ID + expected revision sent for message/verify/judge/purchase/resume as appropriate. Network retry reuses same identity/body; fresh intent gets fresh ID. Conflict refresh with preserved draft, no silent success. Wire paused resume and persisted terminal results. Show accurate reply_mode from saved responses/detail (not hardcoded rules when model used). Display actual returned reward breakdown. Related tests must exercise hooks/request bodies and state transitions, not just rename text.
T5 PG migrations: current integration conftest uses create_all (not migration proof). On a NEW chat_life_* database at127.0.0.1:55437 use actual alembic upgrade head and exercise route with resulting schema; test upgrade from prior revision and preserve a legacy session, downgrade/upgrade ONLY isolated disposable DB. Do not invoke app.main lifespan against default DB: set explicit PG env before imports or use a router-only test app. No existing antifraud_dev.db, no Docker, no production, no commits.

## Required tests before reporting
- Real PG parallel same/different request-ID purchases (one debit), messages (one persisted revision), verifies (no lost evidence), judges (one full atomic payout). Concurrent endpoint invocations with separate DB sessions, NOT sequential requests called concurrency.
- Actual routes: pause, read (no secret), resume same snapshot, terminal, reload exact result; five pause/resume cycles no economic or relation/progress change.
- Actual routes: fresh player cannot finale, wrong contact rejected, all cross-contact prerequisites -> finale reachable, first finale only once; replay updates no progression.
- Actual frontend mutation/retry/409 and paused/resumed/terminal display tests; SDK regeneration and build. Run related tests; document preexisting failures honestly.
- Read docs/agent-work/chat-life/acceptance-probes.md. Run uv, bun per AGENTS. Native PG is currently verified reachable, database chat_life_acceptance_20260919 (chat_test trust) available. Can create another chat_life_* DB there.

Report EXACT remaining failures rather than 'R1-R8 solved'. No broader content rewrite in this assignment. Save report docs/agent-work/chat-life/runtime-repair-report.md, include exact commands/test counts and state the remaining branch/LLM/content work is NOT complete. Keep all code changes in working tree, you remain sole app/test writer.
