# Fix guest entry redirect loop

Project C:\Users\kun\.gemini\antigravity\scratch\antifraud_game. User explicitly reports guest mode returns to login. Root cause login.tsx and signup.tsx set fake demo_token_123; backend rejects; global auth failure redirects.

Implement real anonymous guest entry using existing auth/security/user initialization: POST /login/guest creates a distinct non-superuser guest with server-generated identity and non-guessable random password (no shared account), returns normal signed access token. Guest identity cannot be claimed through password reset/email workflows; mark guest if needed with minimally compatible schema/migration. Prefer reserved unique invalid-domain email and no password exposed; ensure email reset does not send to guests. No accepting fake tokens, no auth bypass, no shared seeded superuser.

Both entry buttons call guest endpoint with loading/error feedback, only navigate after success; clear demo_mode and old fake tokens; actual login and logout also clear stale demo state. Full gameplay must use real guest APIs including journey, quiz and scenario. Do not just suppress 401 or provide static fake homepage. Regenerate SDK. Old demo_token_123 must recover to login once without loops. 403 resource authorization must not clear valid login; only 401 does. Preserve real network errors.

Tests: guest endpoint creates separate least-privilege users and valid tokens; /users/me and /economy/journey work with token; locked contacts still blocked; frontend click success/failure/loading and no fake token; focused tests/build. Use isolated test DB for tests, do not reset existing DB. Do not git checkout/reset/clean, commit/push/deploy. Do not weaken existing tests. Before edits copy touched source files to timestamped temp backup (previous worker accidentally discarded agent.py). Do not touch unrelated dialogue files. Report exact commands and outcomes. Use uv at C:\Users\kun\Documents\ComfyUI\.venv\Scripts\uv.exe and npm exec --yes --package=bun -- bun.
